[project]
authors = [
    {name = "promit_search", email = "ian.herzogpohl@gmail.com"}
]
description = "test"
name = "promit_search"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools"]

[tool.setuptools.packages.find]
where = ["."]
include = ["promit_search*"]
